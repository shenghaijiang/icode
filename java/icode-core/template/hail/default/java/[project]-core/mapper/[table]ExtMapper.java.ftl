package ${model.packageName}.mapper;
import ${model.packageName}.entity.${model.code};
import org.apache.ibatis.annotations.Param;

/**
 * Created by  on ${.now?string('yyyy-MM-dd hh:mm:ss')}
 */

public interface ${model.code}ExtMapper {

}
